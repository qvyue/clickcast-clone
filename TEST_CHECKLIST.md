# 视频颜色对比度测试清单

## 问题描述
网站提取的主题色可能不适合视频使用，导致：
- 深色主题色在暗色背景上不可见
- 低饱和度颜色缺乏辨识度
- 按钮渐变文字颜色选择不当

## 测试场景

### 1. 深色主题色网站 (如 GitHub)
- **输入**: primary="#1E2327" (深灰/近黑)
- **期望**: 自动调整为更亮、更有辨识度的颜色
- **验证**: `ensureVideoSuitableColor('#1E2327')` 应返回亮度 > 30%、饱和度 > 40% 的颜色

### 2. 高对比度渐变按钮 (如黑白渐变)
- **输入**: primary="#FFFFFF", secondary="#000000"
- **期望**:
  - 按钮文字颜色选择黑色
  - 添加文字阴影确保在白色部分也可见
- **验证**: `getButtonTextColor('#FFFFFF', '#000000')` 返回 '#000000'

### 3. 正常品牌色 (如 GitHub 绿)
- **输入**: primary="#2DA44E" (绿色)
- **期望**: 保持不变，颜色适合视频
- **验证**: `ensureVideoSuitableColor('#2DA44E')` 返回原值

### 4. 低饱和度颜色
- **输入**: primary="#808080" (灰色)
- **期望**: 增加饱和度，变成有辨识度的颜色
- **验证**: 输出颜色饱和度 > 40%

### 5. 颜色提取优先级
- **场景**: 页面同时有 CTA 按钮和导航栏背景
- **期望**: 优先提取高饱和度的 CTA 按钮颜色作为主色
- **验证**: 检查 scraped.json 中的 colorPalette.primary

## 回归测试

每次修改颜色相关代码后，运行：
1. 测试颜色调整逻辑
2. 重新生成视频检查输出的 timeline.json
3. 渲染视频确认视觉效果
